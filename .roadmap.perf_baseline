# .roadmap.perf_baseline mk2 — domain SSOT (void perf-first budget tracking), JSONL entry per line
# Header on next line; entries follow.
{"type":"header","kind":"domain","name":"perf_baseline","mk":2,"goal":"speed/memory/GPU-time/syscall budgets first-class concern. Every PR reports Δ vs Ghostty baseline; ≥2% regression blocks merge","required_conditions":[{"id":"perf_baseline.cond.1","desc":"capture Ghostty baseline benches (parse SIMD AVX2/NEON, render Metal/OpenGL, memory arena+screen rings, sys read/write/render thread)","verifier":"test -d /Users/ghost/core/void/src/benchmark && ls src/benchmark | wc -l","status":"partial","evidence":["src/benchmark/ directory exists (inherited from Ghostty)"],"blocker_reason":"Ghostty baseline numbers not yet captured/recorded as void's reference point"},{"id":"perf_baseline.cond.2","desc":"void-specific perf surfaces baseline: tool-call fast path (parser), grid batch reuse (renderer), per-cell allocator (memory), agent-channel thread (sys)","verifier":"src/benchmark grep -c agent_channel","status":"unmet","evidence":[],"blocker_reason":"void perf surfaces not yet built (depends on .roadmap.ai_native_io cond.1+cond.3)"},{"id":"perf_baseline.cond.3","desc":"PR delta reporting — CI workflow runs benches on PR vs main, posts Δ% per surface, blocks merge on ≥2% regression","verifier":"ls /Users/ghost/core/void/.github/workflows | grep -c perf","status":"unmet","evidence":[],"blocker_reason":".github/workflows/perf-budget.yml not yet authored; requires CI runner with consistent perf characteristics"},{"id":"perf_baseline.cond.4","desc":"Build Fork CI (build-fork.yml on macos-15 ARCHS=arm64) is the canonical perf-measurement substrate (DerivedData cache → 3-5× faster from 2nd run)","verifier":"gh api repos/need-singularity/void/actions/workflows | grep -c build-fork","status":"met","evidence":["commit C2 macos-ci 2026-04-22 in .roadmap","Build Fork (macOS) on macos-15 works (per .next-session technical constraints)"],"blocker_reason":""}],"blockers":[{"id":"perf_baseline.blk.1","desc":"namespace runners don't work on this fork (Test.yml pends forever, must cancel each push) — limits CI substrate choice for perf benches","type":"infra","status":"workaround","eta":"","resolution_path":"GitHub-hosted macos-15 runners only (no namespace runners); cancel Test.yml each push (script automation OK per .next-session)"},{"id":"perf_baseline.blk.2","desc":"perf measurement variance on shared CI runners — need ≥3-run median + variance band to set 2% threshold meaningfully","type":"methodology","status":"open","eta":"","resolution_path":"capture variance distribution on 10-run baseline before setting hard threshold"}],"status":"todo","since":"2026-04-21","cross_links":["void/README.md §3 Perf-first table (Ghostty baseline → Void target)","void/.roadmap.ai_native_io cond.3 agent-channel thread (perf surface dependency)","void/AGENTS.md zig build perf hints"]}
