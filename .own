#!raw
# void /.own — L1 project-local rules
# Inherits L0 = hexa-lang /.raw via .raw-ref pin.

since 2026-04-21
why "void = ghostty fork — AI-native terminal, grid mode, perf-first"
inherits hexa-lang/.raw

# pivot 2026-04-21
# - prior plan: hexa-native SSH terminal from scratch
# - new plan:   ghostty fork, rebrand → void
# - base upstream = github.com/ghostty-org/ghostty (see .git remote upstream)
